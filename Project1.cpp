#include <iostream>

int main()

while (!touches the boundary){
    cin pattern 
    for (;the upper row are not all zero; column down)
    shift left or right
    if (row has all 1) {
        clear the row 
        all others 1 goes down 
    }
}